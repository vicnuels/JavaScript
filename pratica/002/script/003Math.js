var numAleatoric = Math.random() * 100
var numRound = Math.round(numAleatoric)

window.document.write("<h1>Número gerado: " + numAleatoric + "<br>")
document.write("Esse número arredondado é: " + numRound + "<h1>")
